# Local Supabase development
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
HEALTH_RECORD_OWNER_ID=

# Android app configuration (safe to bundle; never place a service key here).
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=

# Required for an Ancestry raw export only after you have validated the source metadata.
# Accepted values: GRCh37 or GRCh38. Leave unset rather than guessing.
ANCESTRY_GENOME_BUILD=

# Private web intake access gate
INTAKE_ACCESS_TOKEN=
